<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ru.home.chernyadieva.mybatisdemo.mapper.StudentMapper">

<resultMap id="studentResultMap" type="ru.home.chernyadieva.mybatisdemo.model.Student">
    <result property="id" column="id"/>
    <result property="firstName" column="first_name"/>
    <result property="lastName" column="last_name"/>
    <result property="age" column="age"/>
    <result property="university" column="university"/>
    <result property="faculty" column="faculty"/>
</resultMap>

    <select id="findAll" resultMap="studentResultMap">
        select * from student
    </select>

    <select id="findStudentFromId" parameterType = "int" resultMap="studentResultMap">
        select * from student where id= #{id}
    </select>
    
    <insert id="putNewStudent" parameterType = "Student">
        insert into student (first_name, last_name, age, university, faculty, time_create)
             values(#{firstName}, #{lastName}, #{age}, #{university}, #{faculty}, #{timeCreate})
    </insert>
    
    <update id="updateStudent" parameterType = "Student">
        update student set first_name= #{firstName}, last_name= #{lastName}, age= #{age}, university= #{university},
             faculty= #{faculty}, time_create= #{timeCreate} where id= #{id}
    </update>

    <delete id="deleteStudentFromId" parameterType = "int">
        delete from student where id= #{id}
    </delete>

</mapper>